var React = require('react');

var random = React.createClass({
    render: function() {
        return (
         <div>
             Wow, what a lames project! 
         </div>   
        );
    }
});

module.exports = random;